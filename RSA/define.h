/*****************************************************************//**
 * \file   define.h
 * \brief  Define some definitions for system.
 * 
 * \author dqthebt
 * \date   October 2020
 *********************************************************************/
#pragma once
#define MAX(x, y) ((x) > (y) ? (x) : (y))
#define MIN(x, y) ((x) < (y) ? (x) : (y))
#define NUMS_BIT	512		/* Number bits is supported by the system */
