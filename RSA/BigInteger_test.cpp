#include <gtest/gtest.h>
// #include "BigInteger.h"

TEST(BigIntTest, Add) {
	// BigInteger a("101");
	// bool res = a > BigInteger("0");
	// EXPECT_EQ(true, res);
}

int main(int argc, char **argv) {
    testing::InitGoogleTest(&argc, argv);
    return RUN_ALL_TESTS();
}